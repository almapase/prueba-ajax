<div class="row">
  <h1>New Company</h1>
  <div class="col-md-6">
    <%= render 'form' %>
  </div>
</div>
<div class="row">
  <h1>Listing Companies</h1>

  <table class="table" id="companies">
    <thead>
      <tr>
        <th>Name</th>
        <th>Description</th>
        <th colspan="3">Actions</th>
      </tr>
    </thead>

    <tbody>
      <%= render @companies %>
    </tbody>
  </table>
</div>
<%= render 'modal_edit' %>
